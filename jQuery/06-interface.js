$(document).ready(function(){

    //Draggable: mover elemento por la página
    $(".elemento").draggable();

    //Resizable: redimencionar elementos
    $(".elemento").resizable();

    //
    //$(".lista-seleccionable").selectable(); selecciona los elemenots de una lista
    $(".lista-seleccionable").sortable(); //ordena los elementos de una lista a nuestra manera

    //Dropable
    
})